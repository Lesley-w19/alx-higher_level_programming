#!/usr/bin/python3
# function that prints a string in uppercase followed by a new line.
def islower(c):
    if ord(c_lw) >= 97 and ord(c_lw) <= 122:
        return (True)
    else:
        return (False)
